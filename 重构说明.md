# msrChat 项目重构说明

## 一、项目架构

### 服务端 (server/ChatServer)

纯 C++/Linux 实现，核心壁垒：

```
ChatServer/
├── include/
│   ├── Protocol/
│   │   ├── BaseProtocol.h      ← 协议基类
│   │   └── TLVProtocol.h        ← TLV协议实现
│   ├── AsioIOServicePool.h     ← Asio I/O线程池
│   ├── CServer.h                ← 服务器主类
│   ├── CSession.h               ← 会话管理
│   ├── ThreadPool.h             ← 业务线程池
│   ├── MsgQueue.h               ← 无锁队列
│   ├── FileTransfer.h           ← 文件传输（断点续传）
│   ├── SQLiteMgr.h              ← SQLite存储
│   └── UserData.h               ← JSON用户数据
├── src/
│   ├── main.cpp
│   ├── CServer.cpp
│   ├── CSession.cpp
│   ├── AsioIOServicePool.cpp
│   ├── ThreadPool.cpp
│   ├── MsgQueue.cpp
│   ├── FileTransfer.cpp
│   ├── SQLiteMgr.cpp
│   ├── UserData.cpp
│   └── Protocol/
│       ├── BaseProtocol.cpp
│       └── TLVProtocol.cpp
└── userdata.json                ← 用户数据存储
```

### 客户端 (client/QmsrChat)

Qt 跨平台实现：

```
QmsrChat/
├── include/
│   ├── DbMgr.h                 ← SQLite管理
│   ├── ChatModel.h              ← Model-View架构
│   ├── TcpMgr.h                 ← TCP管理
│   ├── HttpMgr.h                ← HTTP管理
│   └── UserMgr.h                ← 用户管理
└── src/
    ├── DbMgr.cpp
    ├── ChatModel.cpp
    ├── TcpMgr.cpp
    ├── main.cpp
    └── ...
```

## 二、核心模块说明

### 1. TLV 协议层 (Protocol/)

**功能**: 处理 TCP 粘包问题

**协议格式**:
```
[2字节: msg_id][4字节: data_len][N字节: data]
```

**特点**:
- 固定头部 8 字节
- 变长数据域
- 支持最大 10MB 消息

### 2. 线程池 + 无锁队列 (ThreadPool, MsgQueue)

**ThreadPool**: 处理业务逻辑的线程池
- 基于 std::thread
- 支持动态任务提交
- 自动管理线程生命周期

**MsgQueue**: 无锁队列
- 基于 std::atomic + CAS
- 生产者-消费者模式
- 高性能消息传递

### 3. 文件传输 (FileTransfer)

**核心亮点**: 大文件断点续传

**技术实现**:
```cpp
// 小文件(<1MB)：普通 read/write
if (file_size < 1 * 1024 * 1024) {
    read(fd, buffer, size);
    write(client_fd, buffer, size);
}
// 大文件(>1MB)：mmap零拷贝
else {
    void* addr = mmap(nullptr, size, PROT_READ, MAP_SHARED, fd, offset);
    sendfile(client_fd, fd, &offset, size);  // 内核态直接转发
    munmap(addr, size);
}
```

**断点续传机制**:
- 记录已传输字节数
- 重连时从 offset 位置继续
- MD5 校验保证数据完整性

### 4. SQLite 存储层 (SQLiteMgr)

**功能**: 聊天记录本地存储

**表结构**:
```sql
CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    from_uid INTEGER NOT NULL,
    to_uid INTEGER NOT NULL,
    content TEXT NOT NULL,
    timestamp INTEGER NOT NULL,
    status INTEGER DEFAULT 0
);

CREATE INDEX idx_messages_search ON messages(from_uid, to_uid, timestamp DESC);
```

**特点**:
- 事务批量写入提升性能
- 联合索引优化查询
- 支持分页加载

### 5. Model-View 架构 (ChatModel)

**功能**: Qt 列表视图的 MVC 实现

**特点**:
- 继承 QAbstractTableModel
- 支持自定义角色数据
- 实时更新视图

## 三、技术栈总结

| 模块 | 技术 | 场景 |
|------|------|------|
| 网络层 | Asio | 高性能 I/O |
| 协议层 | TLV | TCP 粘包处理 |
| 线程池 | std::thread | 业务逻辑处理 |
| 无锁队列 | std::atomic | 生产者-消费者 |
| 文件传输 | mmap + sendfile | 大文件优化 |
| 存储 | SQLite | 聊天记录 |
| 用户数据 | JSON | 配置存储 |

## 四、编译指南

### 服务端编译

```bash
cd server/ChatServer
mkdir build && cd build
cmake ..
make -j$(nproc)
./ChatServer
```

### 客户端编译

```bash
cd client/QmsrChat
mkdir build && cd build
cmake ..
make -j$(nproc)
./QmsrChat
```

## 五、面试重点

### 1. 服务端技术深度

> "我的项目服务端基于 Asio 实现 Reactor 模型，重点优化了以下方面："
> - **TLV 协议**：解决 TCP 粘包问题，自定义协议头设计
> - **线程池**：业务逻辑与 I/O 分离，提升并发处理能力
> - **无锁队列**：生产者-消费者模式，CAS 保证线程安全
> - **大文件传输**：mmap 零拷贝 + sendfile 内核态转发，突破 I/O 瓶颈

### 2. 客户端工程能力

> "客户端用 Qt 开发，重点展示了跨平台开发能力："
> - **SQLite 本地存储**：事务批量写入优化性能
> - **Model-View 架构**：自定义 QAbstractTableModel 实现聊天列表
> - **QThread 网络线程**：Qt 风格的并发编程

### 3. 场景化技术选型

> "技术选型都是根据场景来的，不是为了用技术而用技术："
> - mmap/sendfile 只在大文件(>1MB)场景使用
> - 线程池处理业务逻辑，无锁队列处理消息传递
> - SQLite 轻量、无外部依赖，适合聊天记录存储

## 六、项目亮点

1. **高性能**: Asio + 线程池 + 无锁队列
2. **高可靠**: 断点续传 + MD5 校验
3. **易扩展**: 模块化设计，职责清晰
4. **跨平台**: Qt 客户端支持多平台
5. **轻量级**: 移除 MySQL/Redis 依赖，使用 SQLite + JSON

## 七、下一步开发建议

### 高优先级
1. 集成现有 ChatServer 代码到新架构
2. 实现消息路由逻辑
3. 完成用户登录/注册功能

### 中优先级
1. 添加心跳保活机制
2. 实现离线消息拉取
3. 文件传输协议完善

### 低优先级
1. 添加用户头像功能
2. 视频播放集成
3. 搜索功能优化

---

**项目定位**: C++ 高性能即时通讯系统，重点展示网络编程、多线程、协议设计能力
