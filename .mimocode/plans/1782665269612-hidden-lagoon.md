# 消息重发机制实现计划

## 问题分析

当前消息发送流程：
1. `ChatController::sendMessage()` 生成 `client_msg_id`，加入 `_pending_messages`
2. 通过 TCP 发送 Protobuf 消息
3. 等待服务器 ACK 响应
4. **30秒超时后直接标记 `status=-1`，不做重发**（`ChatController.cpp:459-478`）

**缺失功能**：
- 无自动重试机制
- 无手动重发入口（点击失败消息重发）

## 实现方案

### 1. 修改 `PendingMessageInfo` 结构体

**文件**: `client/QmsrChat/include/ChatController.h:25-28`

```cpp
struct PendingMessageInfo
{
    qint64 send_time = 0;
    int retry_count = 0;           // 新增：重试次数
    QString content;               // 新增：消息内容（用于重发）
    int to_uid = 0;                // 新增：目标用户（用于重发）
    qint64 original_timestamp = 0; // 新增：原始时间戳（保持幂等性）
};
```

### 2. 添加重试常量和新方法

**文件**: `client/QmsrChat/include/ChatController.h`

```cpp
// 新增常量
static constexpr int MAX_RETRY_COUNT = 3;        // 最大重试次数
static constexpr int RETRY_INTERVAL_SEC = 5;     // 重试间隔（秒）

// 新增方法
Q_INVOKABLE void resendMessage(qint64 timestamp);  // 手动重发
void retryMessage(const QString &client_msg_id);   // 内部重试
```

### 3. 修改 `sendMessage()` 方法

**文件**: `client/QmsrChat/src/ChatController.cpp:200-241`

在 `_pending_messages.insert()` 时填充新字段：
```cpp
_pending_messages.insert(client_msg_id, PendingMessageInfo{
    .send_time = QDateTime::currentSecsSinceEpoch(),
    .retry_count = 0,
    .content = trimmed,
    .to_uid = _target_uid,
    .original_timestamp = msg.timestamp
});
```

### 4. 修改超时清理逻辑（自动重试）

**文件**: `client/QmsrChat/src/ChatController.cpp:459-478`

将 `slotCleanTimeoutMessages()` 改为自动重试逻辑：

```cpp
void ChatController::slotCleanTimeoutMessages()
{
    QMutexLocker locker(&_pending_mutex);
    const qint64 now = QDateTime::currentSecsSinceEpoch();
    const qint64 timeout_threshold = now - MESSAGE_TIMEOUT_SEC;
    const qint64 retry_threshold = now - RETRY_INTERVAL_SEC;
    
    QStringList expired_ids;
    QStringList retry_ids;

    for (auto it = _pending_messages.cbegin(); it != _pending_messages.cend(); ++it)
    {
        if (it.value().send_time < timeout_threshold)
        {
            // 超时且已达最大重试次数 → 标记失败
            if (it.value().retry_count >= MAX_RETRY_COUNT)
                expired_ids.append(it.key());
            // 超时但未达最大重试次数 → 准备重试
            else
                retry_ids.append(it.key());
        }
    }

    // 处理最终失败的消息
    for (const QString &id : expired_ids)
    {
        _pending_messages.remove(id);
        DbThreadManager::Instance().UpdateMessageStatus(id, -1);
        if (_chat_model != nullptr)
            _chat_model->UpdateMessageStatus(id, -1);
    }
    
    locker.unlock();
    
    // 重试消息（解锁后执行，避免死锁）
    for (const QString &id : retry_ids)
        retryMessage(id);
}
```

### 5. 添加 `retryMessage()` 方法

**文件**: `client/QmsrChat/src/ChatController.cpp`（新增方法）

```cpp
void ChatController::retryMessage(const QString &client_msg_id)
{
    PendingMessageInfo info;
    {
        QMutexLocker locker(&_pending_mutex);
        auto it = _pending_messages.find(client_msg_id);
        if (it == _pending_messages.end())
            return;
        info = it.value();
        // 更新重试计数和发送时间
        it.value().retry_count++;
        it.value().send_time = QDateTime::currentSecsSinceEpoch();
    }
    
    // 重新发送（使用相同的 client_msg_id 保证幂等性）
    ChatTextReqStruct req;
    req.from_uid = _current_uid;
    req.to_uid = info.to_uid;
    req.content = info.content;
    req.client_msg_id = client_msg_id;
    req.timestamp = info.original_timestamp;
    TcpMgr::Instance()->slot_send_chat_text_req(req);
    
    // 更新 UI 状态为"重试中"
    if (_chat_model != nullptr)
        _chat_model->UpdateMessageStatus(client_msg_id, 0);
    
    qDebug() << "[ChatController] Retrying message" << client_msg_id 
             << "attempt" << info.retry_count + 1;
}
```

### 6. 添加 `resendMessage()` 方法（手动重发）

**文件**: `client/QmsrChat/src/ChatController.cpp`（新增方法）

```cpp
void ChatController::resendMessage(qint64 timestamp)
{
    // 查找失败的消息
    ChatMessage msg;
    if (_chat_model == nullptr || !_chat_model->GetMessageByTimestamp(timestamp, msg))
    {
        emit sigError(QStringLiteral("消息不存在"));
        return;
    }
    
    // 只能重发失败的消息
    if (msg.status != -1)
    {
        emit sigError(QStringLiteral("该消息无需重发"));
        return;
    }
    
    // 生成新的 client_msg_id（避免与旧的 pending 冲突）
    const QString new_client_msg_id = QUuid::createUuid().toString(QUuid::WithoutBraces);
    
    // 更新模型中的消息状态和 ID
    {
        QMutexLocker locker(&_pending_mutex);
        _pending_messages.insert(new_client_msg_id, PendingMessageInfo{
            .send_time = QDateTime::currentSecsSinceEpoch(),
            .retry_count = 0,
            .content = msg.content,
            .to_uid = msg.to_uid,
            .original_timestamp = msg.timestamp
        });
    }
    
    // 更新模型中的 client_msg_id 和状态
    _chat_model->UpdateMessageClientId(timestamp, new_client_msg_id);
    _chat_model->UpdateMessageStatus(new_client_msg_id, 0);
    DbThreadManager::Instance().UpdateMessageClientId(timestamp, new_client_msg_id);
    
    // 发送消息
    ChatTextReqStruct req;
    req.from_uid = _current_uid;
    req.to_uid = msg.to_uid;
    req.content = msg.content;
    req.client_msg_id = new_client_msg_id;
    req.timestamp = msg.timestamp;
    TcpMgr::Instance()->slot_send_chat_text_req(req);
}
```

### 7. QML 添加手动重发交互

**文件**: `client/QmsrChat/MessageBubble.qml`

在失败状态显示区域添加点击重发功能：

```qml
// 修改 statusIcon 部分
Text {
    id: statusIcon
    visible: isSelf
    font.pixelSize: 10
    font.family: "Microsoft YaHei"
    color: {
        switch (status) {
            case -1: return "#EF4444"
            case 2:  return "#F59E0B"
            default: return "#9C9AAA"
        }
    }
    opacity: status === 0 ? 0.5 : 1.0
    text: {
        switch (status) {
            case 0: return "发送中"
            case 1: return "已送达"
            case 2: return "离线"
            case -1: return "点击重发"
            default: return ""
        }
    }
    
    // 失败消息可点击重发
    MouseArea {
        visible: status === -1
        anchors.fill: parent
        cursorShape: Qt.PointingHandCursor
        onClicked: messageBubble.retryRequested(messageBubble.timestamp)
    }
}

// 新增信号
signal retryRequested(var timestamp)
```

### 8. 信号连接

**文件**: `client/QmsrChat/MessageDelegate.qml`

在 `textBubbleComponent` 中添加信号转发：

```qml
Component {
    id: textBubbleComponent
    MessageBubble {
        // ... existing properties ...
        onRetryRequested: function(ts) {
            chatController.resendMessage(ts)
        }
    }
}
```

### 9. ChatListModel 添加 `UpdateMessageClientId` 方法

**文件**: `client/QmsrChat/include/ChatListModel.h` 和 `src/ChatListModel.cpp`

```cpp
void UpdateMessageClientId(qint64 timestamp, const QString &new_client_msg_id);
```

```cpp
void ChatListModel::UpdateMessageClientId(qint64 timestamp, const QString &new_client_msg_id)
{
    QMutexLocker lock(&_mutex);
    auto it = _timestampIndex.find(timestamp);
    if (it != _timestampIndex.end() && it.value() >= 0 && it.value() < _messages.size())
    {
        _messages[it.value()].client_msg_id = new_client_msg_id;
        // 更新索引
        _clientIdIndex.remove(_messages[it.value()].client_msg_id);
        _clientIdIndex[new_client_msg_id] = it.value();
    }
}
```

### 10. DbService 添加 `UpdateMessageClientId` 方法

**文件**: `client/QmsrChat/include/DbService.h` 和 `src/DbService.cpp`

```cpp
bool UpdateMessageClientId(qint64 timestamp, const QString &new_client_msg_id);
```

实现时执行 SQL：
```sql
UPDATE messages SET client_msg_id = ? WHERE timestamp = ?
```

## 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `include/ChatController.h` | 修改 | 扩展 PendingMessageInfo，添加新方法声明 |
| `src/ChatController.cpp` | 修改 | 实现重试逻辑、手动重发 |
| `MessageBubble.qml` | 修改 | 添加点击重发交互 |
| `MessageDelegate.qml` | 修改 | 转发 retryRequested 信号 |
| `include/ChatListModel.h` | 修改 | 添加 UpdateMessageClientId 声明 |
| `src/ChatListModel.cpp` | 修改 | 实现 UpdateMessageClientId |
| `include/DbService.h` | 修改 | 添加 UpdateMessageClientId 声明 |
| `src/DbService.cpp` | 修改 | 实现 UpdateMessageClientId |

## 验证方法

1. **自动重试测试**：
   - 断开网络 → 发送消息 → 观察控制台日志应显示重试 3 次
   - 重连后验证消息状态更新

2. **手动重发测试**：
   - 发送消息使其失败（如断网等待超时）
   - 点击失败消息的"点击重发"文字
   - 验证消息重新发送并收到 ACK

3. **幂等性测试**：
   - 验证服务器不会收到重复消息（通过 client_msg_id 去重）
